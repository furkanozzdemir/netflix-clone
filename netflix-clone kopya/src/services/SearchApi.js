import axios from "axios";
import React from "react";
import { useDispatch } from "react-redux";
import { setSearchFilm } from "../features/Store";

const SearchApi = ({ movie }) => {
  const dispatch = useDispatch();
};

export default SearchApi;
